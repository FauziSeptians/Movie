export default function ButtonText({ Title }) {
   return (
      <div className="rounded-[50px] bg-[#103d5f] px-6 py-2 text-white font-normal tracking-wider cursor-pointer">
         {Title}
      </div>
   );
}
